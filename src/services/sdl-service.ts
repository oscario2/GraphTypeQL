import { GraphStorage } from '../schema/graph-storage.ts';

export class SDLService {
    /**
     * generate SDL from resolver storage object
     * @param {GraphStorage} resolver
     */
    toSchema(_resolver: GraphStorage) {
        // TODO: implement @oscario2
    }
}
